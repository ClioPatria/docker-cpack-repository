/*  Saved settings
    Date: Mon Jul  6 15:54:12 2015
*/


%	IP addresses from which remotes are allowed to connect
setting(pengines:allow_from, [*]).

%	Keep information to support source-level debugging
setting(swish:debug_info, true).

%	Settings for Docker deployment
setting(cpack:mirrors, 'data/cpack-mirrors').
setting(cliopatria:persistent_store, 'data/RDF-store').
setting(http:logfile, 'data/log/httpd.log').
setting(http:public_port, 443).
